import React from "react";
import { createRoot } from "react-dom/client";
import FocusBoostPrototype from "../focus_bet_pixel_circle_fork.jsx";
import "./index.css";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FocusBoostPrototype />
  </React.StrictMode>
);
